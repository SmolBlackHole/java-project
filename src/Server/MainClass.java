package Server;

import java.io.IOException;
import java.net.Socket;

import Client.Client.Client;

public class MainClass {
    public static void main(String[] args) throws IOException {
        Server.Server.Server.main(args);
    }
}